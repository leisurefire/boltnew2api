# bolt2api v1 Configuration

# Port (default: 8080)
PORT=8080

# Required: Your bolt.new session cookies
# Get these from your browser after logging into bolt.new
# Example: __session=your_session_token; activeOrganizationId=your_org_id; remember_user_token=your_token
BOLT_COOKIES=

# Optional: Default project ID (will be used if not specified in request)
BOLT_PROJECT_ID=49956303

# Environment
NODE_ENV=production
